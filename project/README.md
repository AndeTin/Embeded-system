# Restaurant Search & Navigation Backend

## Setup

1. Install dependencies:
   pip install -r requirements.txt

2. Set up MariaDB and create your database/user.

3. Copy .env.example to .env and fill in your credentials.

4. Run the app:
   python app.py

## TODOs

- Implement user, favorite, and route plan resources
- Add authentication and authorization
- Write unit tests
- Document API endpoints
